if not exist "obj" (
    mkdir obj
    )
if not exist "obj/base" (
    mkdir obj/base
    )
if not exist "obj/web" (
    mkdir obj/web
    )
if not exist "out" (
    mkdir out
    )
if not exist "src/base" (
    mkdir src/base
    )
if not exist "src/web" (
    mkdir src/web
    )